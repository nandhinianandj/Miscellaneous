# Call opencv sift algorithm to identify keypoints(see file in the same folder)
# Draw a region around the image with curve algorithm from points(bezier curve??)
# take that region and create a classifier(logistic/k-means etc..) and compare them with each frame in the video
